---
name: Bug report
about: Something in the plugin does not work
labels: bug
---

## What happened

<!-- What you did, what you expected, and what happened instead. -->

## Environment

- dsh version (`dsh --version`):
- Node.js version (`node --version`):
- OS:
- Plugin version (from the profile's `package.json`):
- Upstream helper version (the pin in `cordis.patch.yml`):

## Does the helper work on its own?

<!-- Running the upstream server directly separates a plugin bug from an
upstream one: npx -y @weadmin/weixin-minigame-helper-mcp@<pin> -->

## Logs

<!-- The `dsh web` console output, and anything from the browser console. -->

## What changed

<!-- One paragraph. Link the issue when there is one. -->

## How it was verified

<!-- The commands you ran and what they showed. A real `dsh web` boot matters
when the change touches slot registration, the stylesheet, or the preview
handshake: jsdom cannot answer for those. -->

## Checklist

- [ ] `pnpm run typecheck`, `pnpm test`, and `pnpm run build` pass.
- [ ] `README.md` and `README.zh.md` are both updated, with matching structure.
- [ ] The `@weadmin/weixin-minigame-helper-mcp` pin is unchanged, or the bump is
      explained above and the skill body still matches upstream's tools.
